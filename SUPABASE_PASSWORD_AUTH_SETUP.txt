CHORE SCORE — PASSWORD-ONLY SUPABASE AUTH

BEFORE UPLOADING THIS BUILD:

Supabase -> Authentication -> Sign In / Providers -> Email
- Email provider: ON
- Confirm Email: OFF

Supabase -> Authentication -> General Configuration
- Allow new users to sign up: ON

Supabase -> Authentication -> Users
- If the old OTP/magic-link testing already created the email account you want to use,
  delete that TEST USER before using Create account in this build.
- The database uses ON DELETE CASCADE, so deleting that user also deletes that user's app_state row.

NO EMAIL TEMPLATE CHANGE IS REQUIRED.
NO OTP CONFIGURATION IS REQUIRED.
NO SMTP CONFIGURATION IS REQUIRED FOR NORMAL ACCOUNT CREATION OR SIGN-IN.

FLOW:
First use: Email + Password -> Create account
Later use: Email + Password -> Sign in

If local browser/PWA storage is erased, sign in again with the same email/password.
The app will restore the cloud app_state row.
